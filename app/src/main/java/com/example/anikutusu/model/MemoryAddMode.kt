package com.example.anikutusu.model

// Enum class to define the different modes for adding a memory
enum class MemoryAddMode {
    SERBEST_EKLE,  // "Free Add" mode – user can add a memory without selecting a specific location
    YERINDE_EKLE   // "Add at Location" mode – user must be at a specific place to add the memory
}
